import React, { FC } from 'react';
import { BaseComponentProps } from 'types';
import './Product.scss';

const Product: FC<BaseComponentProps> = () => {
  return <section className="product"></section>;
};

export default Product;
